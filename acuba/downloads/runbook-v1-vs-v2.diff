# Runbook ALSOL — diff conceptual v1 → v2
# v1 no fue commiteado; este archivo resume los cambios documentados en runbook v2 §12.
# Fecha: 2026-08-27

--- runbook-v1 (desfasado, no commiteado)
+++ runbook-v2 (acuba/downloads/runbook-ingesta-alsol-5-regiones.md @ d699cf0)

@@ contexto @@
- Asumía un solo checkout sin distinguir master vs rama PR
+ §0 Verificación de checkout obligatoria (master vs cursor/alsol-ingest-5-regiones-f938)
+ Tabla de scripts npm por rama

@@ INFOBRAS variables @@
- Solo INFOBRAS_DEPARTAMENTO (singular) documentado como vía principal
+ INFOBRAS_DEPARTAMENTOS (plural, preferido) + INFOBRAS_DEPARTAMENTO (fallback)
+ Nota: crossref:build solo usa singular

@@ INFOBRAS reintentos @@
- "hasta 6 intentos" (sin distinguir rama)
+ master: MAX_ATTEMPTS=4 | rama PR: MAX_ATTEMPTS=6 + fallback curl

@@ scripts npm @@
- coverage:infobras → "no existe"
+ coverage:infobras → SÍ en master (apps/infobras/api/package.json)

- ingest:invierte:full → "no existe"
+ ingest:invierte:full → SÍ en master + scripts/run-invierte-full.ps1

- cobertura:geoserver → "no existe"
+ cobertura:geoserver → SÍ en apps/ceplan-geo/api

- ingest:mef:pilot → documentado como único camino MEF
+ ingest:mef:pilot → solo rama PR
+ ingest:mef + MEF_INGESTA_* / MEF_FILTER_* → camino master (§5.2)

- cobertura:territorial → "no existe"
+ cobertura:territorial → SÍ en master; referencia RUNBOOK_Cobertura_Territorial_ALSOL.md

@@ apps @@
- ceplan-estrategico citado como app geo
+ apps/ceplan-geo (geo) vs apps/ceplan-estrategico (indicadores nacionales)

@@ MEF offsets @@
- Implícito: MEF multiregional funciona igual para todos los deptos
+ §1.4 advertencia explícita: LL offsets confirmados; otros deptos estimados por alfabeto
+ Cajamarca/Cusco requieren validar JSON seccionesSinDatos antes de declarar completo

@@ evidencia @@
- Cifras "verificadas" sin distinción de entorno
+ §5.3 cifras costa marcadas como referencia PR (no evidencia local sin log propio)
+ §4.2, §11 exigen log auditable + SQL antes de actualizar memos

@@ documentación repo @@
- "docs no existen en master"
+ Lista verificable post git pull (matriz, memos, PRD Fase 2)

@@ producto @@
+ §8.2 decisión plantilla memos (v1 vs evidencia runbook)
+ acuba/downloads/brief-producto-sprint6-retoma.md (brief reunión 30 min)

@@ referencias @@
+ docs/RUNBOOK_Cobertura_Territorial_ALSOL.md como runbook canónico de cobertura
+ Commit refs: master @ 78b696f, PR branch @ d699cf0
