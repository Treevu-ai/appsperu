PORT=4027
WEB_ORIGIN=http://localhost:3027
DATABASE_URL=postgres://riesgo_fiscal:riesgo_fiscal@localhost:5459/riesgo_fiscal_isds

# Sin conector de descarga: la fuente (Marco Macroeconómico Multianual del MEF,
# mef.gob.pe/es/marco-macroeconomico/marco-macroeconomico-multianualmmm) publica
# 1-2 ediciones al año, y los PDFs que se intentaron leer no tienen capa de texto
# extraíble por las herramientas disponibles (ver docs/adr/0023). Los datos se
# cargan a mano, edición por edición, vía migraciones SQL versionadas en
# src/db/migrations/ — no hay `npm run ingest:*`.
