# Database
DB_HOST=localhost
DB_PORT=5436
DB_NAME=ceplan_estrategico
DB_USER=ceplan
DB_PASSWORD=ceplan

# API
PORT=4004
WEB_ORIGIN=http://localhost:3004

# ObservaPerú — URL corregida 2026-08-17 (observa.ceplan.gob.pe no resuelve)
OBSERVA_BASE_URL=https://observaperu.ceplan.gob.pe

# Crossref — pool directo a la DB de radar-ejecucion (mismo patrón que infobras/compras-publicas/radar-inversiones)
EJECUCION_DATABASE_URL=postgres://radar:radar@localhost:5432/radar_ejecucion

# Crossref territorial — HTTP a ceplan-geo (Sprint 7 Fase 2)
CEPLAN_GEO_API_URL=http://localhost:4005
