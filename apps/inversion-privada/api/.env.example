PORT=4012
WEB_ORIGIN=http://localhost:3012
DATABASE_URL=postgres://inversion_privada:inversion_privada@localhost:5443/inversion_privada

# Base de radar-inversiones (Invierte.pe) — solo lectura, para el cruce OxI por codigo_snip.
INVERSIONES_DATABASE_URL=postgres://inversiones:inversiones@localhost:5434/radar_inversiones

# Opcional: límite de página para vertixService.php (default 500).
# VERTIX_PAGE_LIMIT=500
