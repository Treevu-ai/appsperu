PORT=4007
WEB_ORIGIN=http://localhost:3007
# Esta app NO tiene base propia — es un agregador de solo lectura sobre las
# 5 bases ya existentes de Follow the Sol. Ver docs/data-contracts/
# salud-institucional-score.md para el detalle de qué componente viene de
# cada una y con qué cruce (exacto o difuso).
EJECUCION_DATABASE_URL=postgres://radar:radar@localhost:5432/radar_ejecucion
INFOBRAS_DATABASE_URL=postgres://infobras:infobras@localhost:5435/infobras
INVERSIONES_DATABASE_URL=postgres://inversiones:inversiones@localhost:5434/radar_inversiones
COMPRAS_DATABASE_URL=postgres://compras:compras@localhost:5433/compras_publicas
FISCAL_DATABASE_URL=postgres://fiscal:fiscal@localhost:5438/identidad_fiscal
